receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318
        include_metadata: true

exporters:
  kafka:
    brokers:
      - redpanda-0:29092

service:
  pipelines:
    logs:
      receivers: [otlp]
      processors: []
      exporters: [kafka]
